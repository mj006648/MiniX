# argocd/bootstrap.yaml

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: minix-root-app
    namespace: argocd
spec:
    project: default
    source:
        repoURL: https://github.com/mj006648/MiniX.git
        targetRevision: main
        path: argocd/minix
    destination:
        server: https://kubernetes.default.svc
        namespace: argocd
    syncPolicy:
        automated:
            prune: true
            selfHeal: false
        syncOptions:
            - CreateNamespace=true
