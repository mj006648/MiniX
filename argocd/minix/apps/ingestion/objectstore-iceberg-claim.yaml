# ingestion/objectstore-iceberg-claim.yaml
apiVersion: ceph.rook.io/v1
kind: CephObjectStore
metadata:
  name: iceberg-store
  namespace: rook-ceph
spec:
  metadataPool:
    failureDomain: host
    replicated:
      size: 3
  dataPool:
    failureDomain: host
    replicated:
      size: 3
  gateway:
    type: s3
    replicas: 1
    port: 8080
    resources:
      requests:
        cpu: "250m"
        memory: "512Mi"
      limits:
        cpu: "500m"
        memory: "1Gi"
  config:
    "rgw_frontends": "beast port=8080"
